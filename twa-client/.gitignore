# Sorties de build et caches Gradle — régénérés par `bubblewrap build`
build/
app/build/
.gradle/
local.properties
*.apk
*.aab
*.idsig
# Clés de signature : le keystore vit dans ../android/ (déjà ignoré)
*.keystore
