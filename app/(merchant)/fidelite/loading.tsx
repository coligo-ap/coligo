// Frontière de chargement : squelette partagé, la nav du layout reste affichée
// (règle CLAUDE.md : chaque page qui await = loading.tsx, tap → réponse immédiate).
export { MerchantPageSkeleton as default } from "@/components/merchant/merchant-page-skeleton";
