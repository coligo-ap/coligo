// Squelette de l'accueil chauffeur (cache froid uniquement : avec le prefetch
// des onglets, la bascule est instantanée quand le cache est chaud). Contenu
// seul — la nav reste affichée par la coque persistante.
export { HomeSkeleton as default } from "@/components/chauffeur/d-skeleton";
